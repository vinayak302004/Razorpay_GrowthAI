// Product tools for the AI agent.
// Keep database access in controlled functions rather than allowing
// the LLM to execute arbitrary queries.

export async function searchProducts(_query) {
  return [];
}
